<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mvn.test.dao.StuDao">
  
    <!-- 查询用户 -->
    <select id="getStu" resultType="com.mvn.test.entity.Stu">
        select id, time from stu where id = #{id}
    </select>
    
</mapper>